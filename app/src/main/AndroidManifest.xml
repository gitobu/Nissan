<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/nissan"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/nissan"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nissan"
        tools:targetApi="31">
        <activity
            android:name=".LocationMapActivity"
            android:exported="false" />
        <activity
            android:name=".LocationListActivity"
            android:exported="false" />
        <activity
            android:name=".VehicleActivity"
            android:exported="false" />
        <activity
            android:name=".StaffActivity"
            android:exported="false" />
        <activity
            android:name=".LocationActivity"
            android:exported="false" />
        <activity
            android:name=".VehicleListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>